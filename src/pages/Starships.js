import Layout from "../components/Layout";

const Starships = () => {
    return (
        <Layout>
            <p>Aquí va la llista de naus</p>
        </Layout>
    );
};

export default Starships;
